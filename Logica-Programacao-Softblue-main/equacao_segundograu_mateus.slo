início {
   real: a;
   real: b;
   real: c;
   real: delta;
   real: x1;
   real: x2;
   
   leia("A", a);
   leia("B", b);
   leia("C", c);
   
   delta = (b * b) - 4 * a * c;
   
   x1 = (-b + rad(delta)) / (2 * a);
   x2 = (-b - rad(delta)) / (2 * a);
   
   escreva("x1 = " + x1) ;
   escreva("x2 = " + x2) ;
  
  }